# Sesión 2026-06-17 — Handoff para la sesión del Mac mini

Resumen de todo lo trabajado hoy en NexUX desde el MacBook (vía GitHub MCP + clon
`~/Nexus`). **Todo está commiteado y pusheado a `main`** (regla #1). Para retomar:
`cd ~/Nexus && git pull`.

> Contexto de infraestructura confirmado hoy: el **colector + motor (poller SMC)
> corren en el VPS de Alemania (Hetzner)**, no en el Mac mini. Railway sirve la web
> pública (`nexux.cl`). El VPS empuja datos a Railway. Ver `deploy/VPS_MIGRACION.md`.

---

## Lo que se hizo (desplegado y verificado en `nexux.cl`)

### 1. Autenticación (Fase A) — ACTIVA
- Google OAuth encendido. Variables en **Railway**: `GOOGLE_CLIENT_ID`,
  `GOOGLE_CLIENT_SECRET`, `SESSION_SECRET`.
- Se aprovisionó **Postgres en Railway** → `DATABASE_URL` referenciada al servicio web.
  El arranque corre `alembic upgrade head` solo (`core/app.py:_run_db_migrations`).
- Admin = `hugoinzunza@gmail.com` (en `core/auth.py`). Beta cerrada por invitación.
- Google Cloud: proyecto **nexux-499714**, pantalla de consentimiento Externa, nombre
  app "Copiloto de Trading" (Google rechaza "Nexus" por marca). Redirect URI:
  `https://nexux.cl/auth/callback` (sin www).
- **Pantalla de login propia** branded: `/login` → `core/login.html`; el salto OAuth
  se movió a `/auth/google`; errores se muestran en la propia pantalla.

### 2. Navegación
- Home (`docs/diseno/home-clean.html`): nav con links reales **Trading / Diario /
  Admin / Salir**, visibles también en móvil (antes el media query <900px los
  escondía → no había cómo entrar a los módulos tras loguearse).
- Acceso directo cruzado **Trading ↔ Diario** en sus barras superiores.

### 3. Ancho desktop (vistas menos angostas)
- Trading `min(95vw, 2200px)`; Diario secciones `.bt` `min(95vw, 1500px)`; Home `min(95vw, 1760px)`.

### 4. Gráfico de Trading (`modules/trading/public/`)
- Etiquetas legibles: `pill()` con texto blanco + punto de color; labels del eje
  (Strong High/Weak Low/EQ) con fondo saturado + texto blanco (`axisLabelTextColor`).
- Menos ruido en niveles: solo los fuertes + el weak más cercano por lado.
- **Tarjeta de Plan destacada** (`.plan-card`): entrada/stop/objetivo/distancia + estado CDC.
- Fix etiqueta FVG (ya no desaparece al mover/zoom; se quitó el umbral de 10px).
- **Explicación de cada indicador al tocarlo** (modal/bottom-sheet) — Régimen, Sesgo,
  Estructura, Sesión, POI, Plan.

### 5. Home poblado (todo PÚBLICO, sin regalar entradas)
- **Radar SMC multi-activo (teaser)**: endpoint `GET /m/trading/api/board` que por
  moneda da sesgo/régimen/POI cercano y si hay setup (`locked`), pero **NO envía
  entry/SL/TP** (eso es el producto pagado). Los setups muestran 🔒 "ver plan" → `/login`.
- **Medidor Fear & Greed** (gauge) + **Heatmap de mercado 24h**.

### 6. Tema oscuro / claro — TODO el producto
- Default **claro** (opt-in a oscuro), recuerda elección en `localStorage` (`nexux-theme`).
- Centralizado: `static/theme.css` (paleta dark-glass) + `static/theme.js` (botón).
- Cableado en Home, Trading, Diario, Login (script anti-parpadeo en cada `<head>`).
- Gráfico: grilla en gris neutro + `--chart-bg` oscuro → se ve bien en ambos temas.

---

## PENDIENTES (lo más importante para la sesión del Mac mini)

1. **Registrar la entrada del profe en el forward-test** (esto NO se ejecutó; requiere
   correr en el VPS de Alemania donde vive el motor + `collector.env`):
   ```bash
   cd ~/Nexus && .venv/bin/python -m modules.trading.add_setup \
     --pair BTC --dir long --entry 63940.6 --sl 63299.8 --tp 67274.3 \
     --label "profe Carlos 17jun M30"
   ```
   Contexto: long del profe (Carlos García, Bitcoin Traders Academy) en BTC, OB de
   descuento sacado en M30. Nuestro motor SÍ lo tenía detectado en **1h** (POI
   63.732–63.871) pero lo deja "en vigilancia" por régimen desfavorable (ADX<25) +
   CDC vencido. La idea es seguir profe (SL ancho) vs nuestro filtro y comparar.

2. **(Opcional) Endpoint remoto para agregar entradas del profe** sin SSH: el flujo es
   one-way (VPS→Railway), así que requeriría que el VPS jale "setups manuales" desde
   Railway en su ciclo. A discutir si las entradas del profe serán recurrentes.

3. **Backtest SMC** (`/m/trading/backtest`, `backtest.html`) — quedó SIN cablear el tema
   oscuro. Sumar: script anti-parpadeo en head + link `theme.css` + `theme.js` + botón.

4. **Roadmap producto** (ver `docs/PRODUCTO_NEXUX.md`): Fase A (Auth) lista. Sigue
   **Fase B (bóveda + conectar exchange)**, C (colector multi-usuario), D (UX), etc.

5. **Más Home** (ideas pendientes, todo info pública, sin entradas): termómetro macro
   (dominancia/ETH-BTC/funding/VIX), mini-gráficos de tendencia por moneda.

---

## Reglas operativas (recordatorio)
- **Todo a GitHub** (ver `CLAUDE.md`). Pull antes, commit+push después.
- El Home **no regala entradas** — solo info que engancha; las entradas son producto
  pagado (freemium). El Radar es público a propósito (anzuelo).
- Correr el motor / `add_setup` / backtests en vivo = **en el VPS de Alemania**.
