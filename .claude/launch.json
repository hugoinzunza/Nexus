{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "nexus",
      "runtimeExecutable": ".venv/bin/python",
      "runtimeArgs": [
        "-c",
        "import os,uvicorn; uvicorn.run('core.app:app', host='0.0.0.0', port=int(os.environ.get('PORT') or 8801), reload=True)"
      ],
      "autoPort": true
    }
  ]
}
