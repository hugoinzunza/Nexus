# Núcleo web de Nexus — FastAPI + uvicorn (alineado con apps/proteq-hub de ClaudeOS).
# El módulo de trading usa solo la librería estándar (urllib) para Crypto.com,
# así que aquí va únicamente lo del núcleo HTTP y el web push (alertas a futuro).
fastapi==0.115.0
uvicorn[standard]==0.32.0
# Web push (las alertas se implementan más adelante; dejamos el patrón listo).
pywebpush==1.14.1
# Graduador SOMBRA de Claude (opcional): gradúa setups para validar su criterio a
# los ~50 trades. Solo se activa si hay ANTHROPIC_API_KEY (Mac mini); en Railway,
# sin key, queda inactivo. El SDK se importa de forma perezosa, así que su ausencia
# no rompe el módulo de trading.
anthropic==0.109.2
# Persistencia multi-tenant (Fase multi-usuario): Postgres vía SQLAlchemy 2.0 SYNC
# (acorde a los handlers síncronos de los módulos) con el driver psycopg3. Solo se usa
# cuando hay DATABASE_URL en el entorno (Railway/web); el VPS/motor y el modo local sin
# DB siguen con los stores JSON (backend elegido por presencia de la variable).
sqlalchemy>=2.0.43,<2.1   # >=2.0.43 por compat con Python 3.14 (el VPS); 2.0.36 rompía al importar
psycopg[binary]==3.2.13
alembic==1.14.0
# Auth multi-usuario (Fase funcional): cookie de sesión FIRMADA (no cifrada, no
# guarda secretos) con itsdangerous. El flujo Google OAuth2 se hace con urllib
# (stdlib), sin SDK extra. Toda la auth queda INERTE hasta configurar las
# variables (GOOGLE_CLIENT_ID/SECRET, SESSION_SECRET): sin ellas el sitio
# funciona como hoy (un usuario, todo abierto).
itsdangerous==2.2.0
# Bóveda (Fase B: conectar exchange): cifrado de sobre AES-256-GCM + RSA-OAEP para
# guardar las API keys de Binance. La pública cifra en Railway; la privada (solo en el
# VPS) descifra. Ya venía transitiva (pywebpush); ahora es dependencia directa.
cryptography>=42.0,<50.0
# Lector local de Vip CoinSignals. Usa una sesion MTProto separada y solo lectura;
# el modulo web recibe snapshots shadow y no conoce credenciales de trading.
telethon>=1.36,<2.0
qrcode[pil]>=7.4,<9.0
